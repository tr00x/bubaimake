## Что изменю
- Уменьшу фото: миниатюры карусели и основное фото.
- Увеличу кнопки (ценник и «Оставить заявку») и выровняю обе по правому краю.
- Уберу подложку у блока с фото — оставлю чистое изображение.
- Починю кнопку «Написать менеджеру» в боковом блоке.
- Удалю лишние секции: «Габариты» и «Расход» вместе с их грид‑контентом.
- Дополирую типографику, отступы и контраст, чтобы выглядело стильно и аккуратно.

## Точные правки по файлам
### `src/pages/Car.tsx`
- Карусель миниатюр:
  - Уменьшу ширину превью: `CarouselItem` с `min-w` → `min-w-[36px] md:min-w-[48px]`.
  - Сохраню `aspect-[4/3]`, не буду скейлить `img`, чтобы картинка оставалась чёткой.
- Основное фото:
  - Удалю фон/бордер/паддинги у обёртки: уберу `bg-neutral-100 border p-4`, оставлю чистый `img` в `aspect-[4/3]` с `rounded-[12px]` и при необходимости мягкую тень.
- Кнопки:
  - «Ценник» на странице: увеличу размеры (`px-[28–32] py-[14–16]`, `rounded-[14]`, `shadow-lg`) без изменения шрифта; стиль — аккуратная пилюля.
  - В липкой шапке: заменю `span` ценника на такую же пилюлю; расположу вместе с «Оставить заявку» справа через `justify-end` и/или `ml-auto`.
  - «Оставить заявку»: увеличу паддинги, приведу к единому стилю с ценником (контраст, тень, ховеры), но оставлю как ссылку.
- Боковой блок «Консультация»:
  - Починю «Написать менеджеру»: обеспечу корректный класс, видимость и hover/active; выровняю по правому краю (`flex justify-end` или `ml-auto`).
- Удаление секций:
  - Полностью удалю заголовки `h2` «Габариты» и «Расход» и их `div`‑гриды.
- Небольшая полировка:
  - Сокращу лишние вертикальные отступы, выровняю сетку, приведу контуры и тени к одной интенсивности.

### `src/components/ui/carousel.tsx`
- Стрелки уже переведены на `span` (←/→); стиль оставляю как на главной (YouTube) — белые круглые 40px с лёгкой тенью.

## Проверка
- Запустить проект и проверить страницу авто:
  - Миниатюры — компактные, чёткие; основное фото без подложки.
  - Ценник и «Оставить заявку» — крупнее, аккуратные, выровнены вправо.
  - «Написать менеджеру» — работает и выглядит консистентно.
  - Секции «Габариты» и «Расход» — отсутствуют, общий вид чище.

Готов выполнить правки сразу и показать результат в превью.